function mostrar()
{
	
	var contador;
	contador=0;

	while(contador<10){

		contador++;// o contador=contador+1
		console.log(contador);
	}



















	/*console.log('iteración while');

	var contador; // primero inicio en 1, segundo se pone condicion para que salga, tercero se debe sumar al contador
	contador=0;		// ejemplo

	while(contador<5){

		console.log("Mensaje: "+contador);
		contador=contador +1;				//para que salga le ponemos que vale uno asi no entra denuevo
		//es lo mismo contador ++; se le suma 1 a la variable.

	}					//cuando termina el while vuelve a ver si se cumple la condicion

	console.log(contador);
	*/
}//FIN DE LA FUNCIÓN